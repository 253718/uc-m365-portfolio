# Teams Phone / Feed

This folder contains the helper used to validate and import a feed-driven Teams Phone run.

## Main helper

- `Import-TeamsPhoneFeedRun.ps1`

## Input

The official demonstration input is the bundled fixture under `tests/Fixtures/feed-sample/`.
