# Tests

The primary test input is the bundled feed fixture under `tests/Fixtures/feed-sample/`.

## Main coverage

- feed contract validation
- feed-driven DAT export
- feed-driven evidence pack
- feed-driven demo wrapper

Run the test suite with:

```powershell
./tooling/Invoke-RepoTests.ps1
```
